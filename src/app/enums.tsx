export enum Views {
  HOME,
  DIALOG,
  ACCOUNT,
}

export enum Direction {
  UP,
  DOWN,
  LEFT,
  RIGHT,
}

export enum eventType {
  KEY_PRESS,
  MOVE_UP,
  MOVE_DOWN,
  MOVE_LEFT,
  MOVE_RIGHT,
  OPEN_LINK,
  OPEN_MODAL,
  CLOSE_MODAL,
  MOBILE_SCREEN,
}